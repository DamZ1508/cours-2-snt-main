[model]
name = Main Page
label = {{ this.title }}

[fields.title]
label = Title
type = string

[fields.body]
label = Body
type = flow
flow_blocks = text-block, html-block, subpages-block

[fields.tags]
label = Tags
type = strings

[children]
order_by = sort_key
